# 💸 Expense Tracker CLI

أداة سطر أوامر (CLI) لإدارة وتتبع المصروفات اليومية، مع دعم الإضافة، التحديث، الحذف، العرض، وتلخيص المصروفات حسب الشهر. يتم حفظ البيانات في ملف `expenses.xlsx` بصيغة مبسطة.

## 🚀 الميزات

- ➕ إضافة مصروف جديد
- 📝 تحديث مصروف موجود
- ❌ حذف مصروف حسب ID
- 📋 عرض جميع المصروفات
- 📊 تلخيص المصروفات حسب الشهر أو بشكل عام
- 📁 حفظ البيانات في ملف Excel بسيط

## 🛠️ المتطلبات

- Node.js >= 18
- مكتبة `commander` لإدارة الأوامر
- ملف `expenses.xlsx` يتم إنشاؤه تلقائيًا إذا لم يكن موجودًا

## 📦 التثبيت

```bash
npm install
```
## ▶️ الاستخدام

```bash
npx tsx index.ts <command> [options]
```
## 🧪 أمثلة

إضافة مصروف:

```bash
expense-tracker add -d "Lunch" -m 50
```

عرض جميع المصروفات:

```bash
expense-tracker list
```

تحديث مصروف:

```bash
expense-tracker update -i 3 -d "Dinner" -m 80
```

حذف مصروف:

```bash
expense-tracker delete -i 2
```

تلخيص المصروفات لشهر معين:

```bash
expense-tracker summary --month 11
```

تلخيص جميع المصروفات:

```bash
expense-tracker summary
```

## 📂 هيكل المشروع

```
├── constants.js                # يحتوي على أسماء الأعمدة والتواريخ
├── utils/
│	├── toExcelFormat.util.js  # تحويل البيانات لصيغة Excel
│	└── toJSFormat.util.js     # تحويل البيانات لصيغة JavaScript
├── index.ts                   # الكود الرئيسي للأوامر
├── expenses.xlsx              # ملف المصروفات
└── README.md
```

